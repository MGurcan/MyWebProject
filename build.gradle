plugins {
  id "org.sonarqube" version "3.3"
}

sonarqube {
  properties {
    property "sonar.projectKey", "MGurcan_MyWebProject"
    property "sonar.organization", "mgurcan"
    property "sonar.host.url", "https://sonarcloud.io"
  }
}